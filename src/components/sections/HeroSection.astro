---
import type { Event } from '../../content.config';

interface Props {
  event?: Event;
  mode?: 'global' | 'event';
  nextEvent?: {
      id: string;
      data: Event;
  };
  cityName?: string;
}

const { event, mode = 'event', nextEvent, cityName } = Astro.props;
---

{mode === 'global' ? (
  <div class="relative isolate overflow-hidden bg-slate-900">
    <div class="absolute inset-0 -z-10 bg-gradient-to-br from-slate-900 via-slate-900 to-slate-800"></div>

    <div class="mx-auto max-w-7xl px-6 lg:px-8 py-10 sm:py-12">
      <div class="text-center max-w-3xl mx-auto">
        <h1 class="text-3xl font-bold tracking-tight text-white sm:text-4xl lg:text-5xl">
          The Premier Global <span class="text-primary-400">LLM Conference</span>
        </h1>
        <p class="mt-4 text-base text-slate-300 max-w-2xl mx-auto">
          Join the world's leading AI engineers, researchers, and founders. Deep dives into Large Language Models, Generative AI, and Machine Learning.
        </p>
      </div>
    </div>
  </div>
) : (
  <div class="relative isolate overflow-hidden bg-slate-900 border-b border-white/10">
    <div class="absolute inset-0 -z-10 bg-gradient-to-r from-slate-900 to-slate-800"></div>
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-6 sm:py-8">
      <div class="flex flex-col md:flex-row md:items-end justify-between gap-4">
        <div>
          <h1 class="text-2xl font-bold tracking-tight text-white sm:text-3xl">
            LLMday {cityName}
          </h1>
          <p class="mt-1 text-sm text-slate-400">
            {event?.dateString} · {event?.locationString}
          </p>
        </div>

        {event?.state !== 'after' && event?.lumaEventId && (
          <a href="#tickets" class="rounded-full bg-primary-600 px-5 py-2 text-sm font-semibold text-white shadow-sm hover:bg-primary-500">
            Get Tickets
          </a>
        )}
      </div>
    </div>
  </div>
)}
